% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xenium_proximity.R
\name{analyze_xenium_cell_type_proximity}
\alias{analyze_xenium_cell_type_proximity}
\title{Analyze spatial proximity between Xenium cell types}
\usage{
analyze_xenium_cell_type_proximity(
  cds,
  cell_type_col,
  sample_col = "sample",
  x_col = "x_centroid",
  y_col = "y_centroid",
  k = 10,
  max_distance = 50,
  include_same_type = TRUE,
  chunk_size = 1000,
  return_neighbors = TRUE
)
}
\arguments{
\item{cds}{A monocle3 cell_data_set with Xenium centroid coordinates and cell
type annotations in \code{colData(cds)}.}

\item{cell_type_col}{Column in \code{colData(cds)} containing cell type labels.}

\item{sample_col}{Column in \code{colData(cds)} identifying independent Xenium
samples. Nearest neighbors are only searched within the same sample. Set to
\code{NULL} to analyze all cells together.}

\item{x_col, y_col}{Columns in \code{colData(cds)} containing centroid
coordinates in microns.}

\item{k}{Number of nearest neighbors to keep per cell.}

\item{max_distance}{Optional maximum centroid distance in microns. Neighbor
links farther than this value are discarded.}

\item{include_same_type}{Logical; if \code{FALSE}, neighbors with the same
cell type as the source cell are excluded from the reported summaries.}

\item{chunk_size}{Number of source cells processed at once when calculating
distances. Lower values use less memory.}

\item{return_neighbors}{Logical; if \code{TRUE}, include the per-cell nearest
neighbor table in the returned list.}
}
\value{
A list with:
\describe{
  \item{\code{summary}}{Per-sample source-to-target cell type proximity
  summary with neighbor counts and distance statistics.}
  \item{\code{overall_summary}}{The same statistics pooled across samples.}
  \item{\code{closest_cell_types}}{For each sample and source cell type, the
  target cell type with the smallest median nearest-neighbor distance.}
  \item{\code{neighbors}}{Returned only when \code{return_neighbors = TRUE};
  one row per retained source-neighbor link.}
}
}
\description{
Deprecated compatibility wrapper around \code{analyze_xenium_cell_proximity()}.
Use \code{analyze_xenium_cell_proximity()} for pairwise enrichment summaries,
stratified abundance summaries, and plotting support.
}
