% Generated by roxygen2: do not edit by hand
\name{select_xenium_rois}
\alias{select_xenium_rois}
\title{Select rectangular regions of interest from large Xenium samples}
\usage{
select_xenium_rois(
  cds,
  sample_ids = NULL,
  n_rois = 5,
  roi_width_um = 1000,
  roi_height_um = 1000,
  strategy = c("quantile_grid", "random"),
  min_cells = 100,
  sample_col = "sample",
  x_col = "x_centroid",
  y_col = "y_centroid",
  seed = 12345,
  grid_n = 25
)
}
\arguments{
\item{cds}{A monocle3 cell_data_set with Xenium centroid coordinates in \code{colData(cds)}.}
\item{sample_ids}{Optional character vector of samples to include. Defaults to all samples in \code{sample_col}.}
\item{n_rois}{Number of ROIs to choose per sample.}
\item{roi_width_um, roi_height_um}{ROI width and height in microns.}
\item{strategy}{ROI placement strategy. \code{"quantile_grid"} scores a grid of candidate windows and chooses windows spread across cell-density quantiles; \code{"random"} samples candidate windows at random.}
\item{min_cells}{Minimum cells required in an ROI candidate.}
\item{sample_col, x_col, y_col}{Column names in \code{colData(cds)}.}
\item{seed}{Random seed used for tie-breaking and random sampling.}
\item{grid_n}{Number of candidate window centers per axis for \code{"quantile_grid"}.}
}
\value{A data.frame with one row per selected ROI, including sample, bounds, center coordinates, and number of cells.}
\description{Creates reproducible, per-sample rectangular regions of interest (ROIs) from Xenium cell centroid coordinates.}
